
<view class="lite animation-slide-bottom" style="animation-delay: {{(index)*0.1}}s;">
  <view class="cont">
    <image class="img" mode="widthFix" src="{{items.img_url}}" />
    <view class="title">{{items.name}}</view>
  </view>
</view>